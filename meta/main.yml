---
galaxy_info:
  author: biomancer
  description: Letsencrypt installation and config with web-root authenticator
  license: MIT
  min_ansible_version: 1.2
  platforms:
    - name: Ubuntu
      versions:
        - trusty
  categories:
    - networking
    - system
    - web
dependencies: []
  # List your role dependencies here, one per line. Only
  # dependencies available via galaxy should be listed here.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
  
